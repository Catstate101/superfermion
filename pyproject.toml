[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[tool.maturin]
manifest-path = "crates/sf-bindings/Cargo.toml"
module-name = "superfermion._sf_core"
python-source = "."
features = ["pyo3/extension-module"]

[project]
name = "superfermion"
version = "0.1.6"
description = "High-performance quantum computing framework with a Python API and Rust simulation core"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [
    {name = "Faisal Ahmed Sifat", email = "faisalahmed531@gmail.com"},
    {name = "Tamim Muhebbullah", email = "cntgtamim@gmail.com"},
]
keywords = [
    "quantum", "machine-learning", "jax", "quantum-computing",
    "variational", "VQE", "QAOA", "differentiable", "quantum-ml",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24.0",
]

[project.optional-dependencies]
jax = [
    "jax>=0.4.0",
    "jaxlib>=0.4.0",
]
qml = [
    "superfermion[jax]",
    "flax>=0.8.0",
    "optax>=0.2.0",
]
dev = [
    "pytest>=7.0",
    "pytest-benchmark>=4.0",
    "pytest-cov>=4.0",
    "pytest-timeout>=2.0",
    "black>=23.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]
gpu = [
    "superfermion[jax]",
    "jax[cuda12]>=0.4.0",
]
qpu = [
    "qiskit-ibm-runtime>=0.20",
    "amazon-braket-sdk>=1.60",
]
benchmarks = [
    "pennylane>=0.45.0",
    "qiskit-aer>=0.14",
    "qiskit>=1.0",
    "pandas>=2.0",
    "matplotlib>=3.7",
    "psutil>=5.0",
]
ml = [
    "torch>=2.0",
    "tensorflow>=2.15; python_version < '3.13'",
    "cirq>=1.3",
]
viz = [
    "matplotlib>=3.7",
]
chemistry = [
    "scipy>=1.10.0",
    "pyscf>=2.3",
]
docs = [
    "sphinx>=7.0",
    "myst-parser>=2.0",
    "sphinx-book-theme>=1.0",
]
all = [
    "superfermion[qml,dev,docs,gpu,qpu,viz,chemistry,benchmarks,ml]",
]

[project.urls]
Repository = "https://github.com/Catstate101/superfermion"
Issues = "https://github.com/Catstate101/superfermion/issues"
Documentation = "https://superfermion.com/docs"
Changelog = "https://github.com/Catstate101/superfermion/blob/main/CHANGELOG.md"

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.cibuildwheel]
# Skip musllinux and 32-bit builds — we only target glibc (manylinux), macOS, and Windows
skip = ["*-musllinux_*", "*-manylinux_i686", "*-win32"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
filterwarnings = [
    "ignore::pytest.PytestReturnNotNoneWarning",
    # Upstream Qiskit 2.1 → 3.0 migration: class-form feature maps / n-local
    # circuits are being replaced by functions.
    "ignore:The class ``qiskit\\.circuit\\.library.*:DeprecationWarning",
    # Upstream flax: SelfAttention → MultiHeadDotProductAttention migration.
    "ignore:SelfAttention will be deprecated soon.*:DeprecationWarning",
    # Upstream PennyLane: shots-on-device → set_shots transform migration.
    "ignore:Setting shots on device is deprecated.*",
    # Upstream scipy 1.18: L-BFGS-B `disp`/`iprint` deprecation.
    "ignore:.*`disp` and `iprint` options.*:DeprecationWarning",
]
